'use strict';

module.exports = app => {
    // var server;
    // app.on('server', workerServer => {
    //     server = workerServer;
    // });
    // app.ready(async () => {
    //     if (server) {
    //         app.sharedb.init(server, app);
    //     } else {
    //         setTimeout(() => { app.sharedb.init(server, app) }, 5000);
    //     }
    // });
};